#include "Geometry.h"

Cordinate::Cordinate(int x, int y) {
	Cordinate::x = x;
	Cordinate::y = y;
}

CenterRatio::CenterRatio(double x, double y) {
	CenterRatio::x = x;
	CenterRatio::y = y;
}

Size::Size(int x, int y)
{
	Size::x = x;
	Size::y = y;
}
